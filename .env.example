# Search APIs
# Google Custom Search - REQUIRED
# Get these from: https://developers.google.com/custom-search/v1/overview
# 1. Create a Custom Search Engine: https://programmablesearchengine.google.com/
# 2. Get API Key: https://console.cloud.google.com/apis/credentials
GOOGLE_CSE_ID=your_google_custom_search_engine_id
GOOGLE_API_KEY=your_google_api_key

# Bing Search API - OPTIONAL (requires Azure subscription)
# BING_API_KEY=your_bing_search_api_key

# GitHub - REQUIRED for code search
# Create a token at: https://github.com/settings/tokens
# Use 'Classic' token with 'public_repo' scope for public repositories
# OR use 'Fine-grained' token with 'Contents: Read' and 'Metadata: Read' permissions
GITHUB_TOKEN=your_github_personal_access_token

# Server Configuration
PORT=3000
NODE_ENV=development

# Storage Configuration
CACHE_DB_PATH=.data/cache.sqlite
REPORTS_DIR=reports

# Optional: Puppeteer Configuration
# PUPPETEER_EXECUTABLE_PATH=/path/to/chrome

# Rate Limiting (requests per second)
RATE_LIMIT_GLOBAL=10
RATE_LIMIT_PER_DOMAIN=2

# Timeouts (in milliseconds)
REQUEST_TIMEOUT=30000
SCRAPE_TIMEOUT=60000

# Concurrency
MAX_CONCURRENT_SEARCHES=3
MAX_CONCURRENT_SCRAPES=5

# OpenAI Configuration (for evaluator.py)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key
# Optional: Specify model (defaults to gpt-3.5-turbo)
# OPENAI_MODEL=gpt-4
